import React from 'react'
import { RoomsSvg } from './roomsSvg'

interface Props { }

export const Rooms = (props: Props) => {
    const { } = props

    return (
        <RoomsSvg></RoomsSvg>
    )
}