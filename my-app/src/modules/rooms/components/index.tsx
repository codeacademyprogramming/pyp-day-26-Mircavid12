import React from 'react'
import { useSelector } from 'react-redux'
import { IRoom } from '../../modules/models'
import { Rooms } from './room'

interface Props { }
export const RoomsPage = (props: Props) => {
    const { } = props

    return (
        <Rooms> </Rooms>
    )
}